name: CI - Gran
run-name: CI - Gran - ${{ github.event.head_commit.message }}

on:
  push:
    branches:
      - 'trunk'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project sources
        uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle
      - name: Build with Gradle
        run: ./gradlew build
